Steps to run this rogram
step 1 : Install this project from github
step 2 : Make sure that you are in linux os environment
step 3 : make sure gcc is installed check by gcc -- version , if not there pleaase install it 
step 4 : make sure that raylib is installed if not plese install it
step 5 : now open this folder in the coding environment(eg vscode) next run these commandes
         make             # to make the output(compile)
        ./thermal_monitor # output name
        make clear        # to clear the output 
